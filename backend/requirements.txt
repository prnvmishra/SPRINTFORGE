fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
httpx==0.28.1
email-validator==2.2.0
psycopg[binary]==3.2.3

# Rendered HTML/CSS grading (app/services/render_judge.py). The browser binary
# is a separate step: `playwright install chromium`. Without it the rendered
# checks fail loudly — see docs/rendered_checks.md.
playwright==1.62.0

# --- development / tests ---------------------------------------------------
# The deterministic validator also needs Node.js 18+ on PATH and the vendored
# AST tool's dependencies: `cd app/tools/js_ast && npm install`.
pytest==8.3.4
